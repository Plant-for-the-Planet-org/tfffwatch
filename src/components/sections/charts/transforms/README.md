# charts/transforms/

Scaffold for the architecture phase (candidate C04). Will hold pure `(records, dataset) → ChartData` functions (no React) extracted from the inline `useMemo` blocks in the chart components. This becomes the unit-test surface. Empty for now. See `docs/refactor/04-architecture.md`.
